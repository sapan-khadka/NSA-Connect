[pytest]
pythonpath = .
testpaths = tests
markers =
    empty_org: skip seeding a fixture org owner (first-registrant bootstrap tests)
filterwarnings =
    ignore:'asyncio.iscoroutinefunction' is deprecated and slated for removal in Python 3.16:DeprecationWarning:slowapi.extension
