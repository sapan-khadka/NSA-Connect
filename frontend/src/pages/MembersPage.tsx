import { PlaceholderPage } from "../components/PlaceholderPage";

export function MembersPage() {
  return (
    <PlaceholderPage
      title="Members"
      description="TODO: list members, approve/reject, manage roles (board+ only)."
    />
  );
}
