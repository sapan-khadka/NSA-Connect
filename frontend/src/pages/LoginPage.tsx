import { PlaceholderPage } from "../components/PlaceholderPage";

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="TODO: authenticate member and store JWT token."
    />
  );
}
